name: "Platform Destroy"
on:
  workflow_dispatch:
    
jobs:
  destroy:
    runs-on: ubuntu-latest
    env:  
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}  
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }} 
      TF_VAR_argocd_admin_password_hash: ${{ secrets.ARGO_CD_ADMIN_PASS }}
      TF_VAR_grafana_admin_password: ${{ secrets.GRAFANA_ADMIN_PASS }}  

    steps:
      - uses: actions/checkout@v4

      - name: Terraform Setup
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init - Argo
        run: terraform init
        working-directory: Argo/

      - name: Terraform Destroy - Argo
        run: terraform destroy -auto-approve
        working-directory: Argo/

      - name: Terraform Init - Platform
        if: always()
        run: terraform init
        working-directory: Platform/

      - name: Terraform Destroy - Platform
        if: always()
        run: terraform destroy -auto-approve
        working-directory: Platform/